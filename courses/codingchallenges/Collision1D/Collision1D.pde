Ball b1, b2;

void setup(){
  size(800,600);
  // initialize balls, you don't need to randomize
  // create the balls manually so they don't overlap.
  
    
}

void draw(){
  background(255);
  
  
  
}

void collision(Ball b1, Ball b2){
  // find distance between balls
  
  
  // if balls are close
  // calculate velocities after collision
  // (Hint: Use temporary variables. Do you see why?)
  
}
