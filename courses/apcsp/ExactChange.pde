// Exact Change Lab

int totalCents, remaining;

// declare variables for quarters, dimes, nickels and pennies.

String exactChange; 

void setup(){
  size(600,600);
  textAlign(CENTER);
  
  //initialize totalCents
  totalCents = 122;
  // compute number of quarters and the remaining amount.

  
  // compute number of dimes and the remaining amount.
  
  // compute number of nickels and the remaining amount.

  // compute the number of pennies.
  
  // String concatenate your answers above to form one String
  // Store your answer in exactChange variable.
  // with the following format: 
  // 2 quarters 2 dimes 0 nickels 4 pennies
  
}

void draw(){
  background(255);
  fill(0); // black text
  textSize(22);
  // display the total cents in the center of the screen.
  
  // display exactChange string below total cents. 

}
