// Create a random number of balls of varying sizes.

int numBalls = 100;


// create four integer arrays: x, y, xspeed, diameter
// each of length numBalls


// create integer variable indexLargest, initialize to 0


void setup(){
  size(600,600);
  
  // use for loop to randomize each element of 
  // the four arrays above. 
  // use random(value1, value2)
  // make sure there are no zero xspeed
  
  
  // call indexLargestDiameter() to get the 
  // index of the largest ball.
  // store this in indexLargest variable.
  
}

void draw(){
  //background
  
  //fill a color
  
  // use for loop to move ball(left/right only), draw
  // each ball AND move each ball.
  
  
  // now draw ball with largest diameter
  // with different color.



}

// this method returns the index of the largest ball
// if there are multiple largest balls, return index
// of the first largest one.
int indexLargestDiameter()
{
  
}
